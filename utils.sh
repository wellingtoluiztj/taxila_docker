#docker_id=$1

#docker run -d -P --name taxila2 $docker_id ##rodar o docker

#echo `sudo docker port test_sshd_container` ##testar qual porta o docker usa

#link https://adamtheautomator.com/ssh-into-docker-container/

#echo `docker inspect taxila2 | grep 'IPAddress'` ##ip docker para conectar via ssh
